import os

p1_pid =  os.fork()
p2_pid  = os.fork()
p3_pid = os.fork()

if(p1_pid>0):
	print(f"Parent Process: My PID is {os.getpid()}, and i created child pro>
elif(pid==0):
        print(f"Child  Process: My PID is {os.getpid()}, and my parent's PID is >
else:
       print("No PID")
